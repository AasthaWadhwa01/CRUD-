/*var mongoose= require('mongoose');

var Schema= mongoose.Schema;

var studentSchema = new Schema({
	name: String;
});

module.exports = mongoose.model

var studentModel = mongoose.model('studentModel',studentModel)*/